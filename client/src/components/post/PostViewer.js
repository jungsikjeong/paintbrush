import React from 'react';
import styled from 'styled-components';

const PostViewerContainer = styled.div`
  background: white;
`;

const PostViewer = () => {
  return <PostViewerContainer>PostViewerContainer</PostViewerContainer>;
};

export default PostViewer;
